# protoc-gen-go-gin

protoc-gen-go-gin is a protoc plugin that generates gin http server code from a proto file.

## Features

- Generate gin HTTP server code from proto files with `google.api.http` annotations
- Support for path parameters (`:id` style)
- Automatic query parameter binding with snake_case alias support
- Request body binding for POST/PUT/PATCH methods
- Auto-call `Validate()` method if present on request messages
- Middleware support via operation names

## Install

```shell
$ go install github.com/trumanwong/protoc-gen-go-gin@latest
```

## Usage

api/app/v1/demo.proto

```proto    
syntax = "proto3";

package api.app.v1;

option go_package = "gin-layout/api/app/v1;v1";
import "google/api/annotations.proto";

service Demo {
	rpc GetDemo (GetDemoRequest) returns (GetDemoReply) {
		option (google.api.http) = {
			get: "/api/demo/:id"
		};
	};

	rpc CreateDemo (CreateDemoRequest) returns (CreateDemoReply) {
		option (google.api.http) = {
			post: "/api/demo"
			body: "*"
		};
	};
}

message GetDemoRequest {
	uint64 id = 1;
	string user_name = 2;
	int32 page_size = 3;
}
message GetDemoReply {}

message CreateDemoRequest {
	string name = 1;
	string user_name = 2;
}
message CreateDemoReply {}
``` 

```shell
$ protoc --proto_path=. --proto_path=./third_party --go_out=paths=source_relative:. \
    --go-grpc_out=paths=source_relative:. \
    --go-gin_out=paths=source_relative:. \
    --openapi_out=paths=source_relative:. \
    api/app/v1/demo.proto
```

## Query Parameter Binding

The generated code automatically binds query parameters for all HTTP methods. It also supports **snake_case query parameter names**, which are automatically mapped to the corresponding camelCase/PascalCase struct fields.

For example, given the `GetDemoRequest` message above:

```proto
message GetDemoRequest {
	uint64 id = 1;
	string user_name = 2;
	int32 page_size = 3;
}
```

All of the following query styles are supported:

```
GET /api/demo/1?user_name=foo&page_size=10
GET /api/demo/1?userName=foo&pageSize=10
GET /api/demo/1?UserName=foo&PageSize=10
```

This means clients can use the proto-native snake_case field names in query parameters, and they will be correctly bound to the Go struct fields.

### How it works

The generated handler uses a `bindQueryWithSnakeCaseAlias` function that:

1. Reads all query parameters from the request URL
2. For each snake_case key (e.g. `page_size`), adds aliases in lowerCamelCase (`pageSize`) and UpperCamelCase (`PageSize`)
3. Calls `ShouldBindQuery` with the normalized parameters

This binding is applied to **all HTTP methods** (GET, POST, PUT, DELETE, PATCH), so query parameters can be used alongside a JSON body for POST/PUT/PATCH requests.

## Generated Code Example

```go
// Code generated by protoc-gen-gin-http. DO NOT EDIT.

package v1

import (
	context "context"
	gin "github.com/gin-gonic/gin"
	transport "github.com/trumanwong/gin-transport/v2/transport"
	errors "github.com/trumanwong/gin-transport/v2/transport/errors"
	url "net/url"
	reflect "reflect"
)

var _ = new(context.Context)
var _ = url.ParseQuery
var _ = errors.New
var _ = gin.Version
var _ = reflect.ValueOf
var _ = transport.SupportPackageIsVersion1

const OperationDemoGetDemo = "/api.app.v1.Demo/GetDemo"
const OperationDemoCreateDemo = "/api.app.v1.Demo/CreateDemo"

type DemoHTTPServer interface {
	GetDemo(context.Context, *GetDemoRequest) (*GetDemoReply, error)
	CreateDemo(context.Context, *CreateDemoRequest) (*CreateDemoReply, error)
}

func RegisterDemoHTTPServer(s *transport.Server, srv DemoHTTPServer) {
	s.AddMethod("GET", "/api/demo/:id", _Demo_GetDemo0_HTTP_Handler(s, srv, OperationDemoGetDemo))
	s.AddMethod("POST", "/api/demo", _Demo_CreateDemo0_HTTP_Handler(s, srv, OperationDemoCreateDemo))
}

func bindQueryWithSnakeCaseAlias(ctx *gin.Context, req interface{}) error {
	values := ctx.Request.URL.Query()
	if len(values) == 0 {
		return nil
	}

	normalized := make(url.Values, len(values))
	changed := false
	for k, vs := range values {
		copied := make([]string, len(vs))
		copy(copied, vs)
		normalized[k] = copied

		lowerCamel := snakeToLowerCamel(k)
		upperCamel := upperFirst(lowerCamel)
		if lowerCamel != "" && lowerCamel != k {
			if _, exists := normalized[lowerCamel]; !exists {
				normalized[lowerCamel] = copied
				changed = true
			}
		}
		if upperCamel != "" && upperCamel != k {
			if _, exists := normalized[upperCamel]; !exists {
				normalized[upperCamel] = copied
				changed = true
			}
		}
	}

	if changed {
		ctx.Request.URL.RawQuery = normalized.Encode()
	}
	return ctx.ShouldBindQuery(req)
}

func _Demo_GetDemo0_HTTP_Handler(s *transport.Server, srv DemoHTTPServer, operation string) func(ctx *gin.Context) {
	return func(ctx *gin.Context) {
		for _, f := range s.GetMiddlewares() {
			if err := f(ctx, operation); err != nil {
				s.ResultError(ctx, err)
				return
			}
		}
		var req GetDemoRequest
		if err := bindQueryWithSnakeCaseAlias(ctx, &req); err != nil {
			s.ResultError(ctx, err)
			return
		}
		if err := ctx.ShouldBindUri(&req); err != nil {
			s.ResultError(ctx, err)
			return
		}
		if validate := reflect.ValueOf(&req).MethodByName("Validate"); validate.IsValid() {
			if err := validate.Call(nil)[0].Interface(); err != nil {
				s.ResultError(ctx, errors.New(400, "", "validate fail: "+err.(error).Error()))
				return
			}
		}
		reply, err := srv.GetDemo(ctx, &req)
		if err != nil {
			s.ResultError(ctx, err)
			return
		}
		s.Result(ctx, 200, reply)
	}
}

func _Demo_CreateDemo0_HTTP_Handler(s *transport.Server, srv DemoHTTPServer, operation string) func(ctx *gin.Context) {
	return func(ctx *gin.Context) {
		for _, f := range s.GetMiddlewares() {
			if err := f(ctx, operation); err != nil {
				s.ResultError(ctx, err)
				return
			}
		}
		var req CreateDemoRequest
		if err := ctx.ShouldBindJSON(&req); err != nil {
			s.ResultError(ctx, err)
			return
		}
		if err := bindQueryWithSnakeCaseAlias(ctx, &req); err != nil {
			s.ResultError(ctx, err)
			return
		}
		if validate := reflect.ValueOf(&req).MethodByName("Validate"); validate.IsValid() {
			if err := validate.Call(nil)[0].Interface(); err != nil {
				s.ResultError(ctx, errors.New(400, "", "validate fail: "+err.(error).Error()))
				return
			}
		}
		reply, err := srv.CreateDemo(ctx, &req)
		if err != nil {
			s.ResultError(ctx, err)
			return
		}
		s.Result(ctx, 200, reply)
	}
}
```

## Run Example

main.go

```go
package main

import (
	"fmt"
	v1 "gin-layout/api/app/v1"
	"github.com/gin-gonic/gin"
	"github.com/trumanwong/gin-transport/v2/transport"
)

func main() {
	engine := gin.New()
	engine.Use(gin.Recovery())
	server := transport.NewServer(engine, []*transport.GroupMiddleware{
		// Middleware for operation
		//{
		//	Middleware: func(ctx *gin.Context) {
		//
		//	},
		//	Operations: []string{v1.OperationDemoGetDemo},
		//},
	}, []string{
		fmt.Sprintf(":%d", 8080),
	})
	v1.RegisterDemoHTTPServer(server, v1.UnimplementedDemoServer{})
	err := server.Run()
	if err != nil {
		panic(err)
	}
}
```

## License

This project is licensed under the MIT License - see the [LICENSE.md](https://github.com/trumanwong/protoc-gen-go-gin/blob/main/LICENSE) file for details.

## Acknowledgements

The following project had particular influence on protoc-gen-go-gin's design.

- [go-kratos/kratos](https://github.com/go-kratos/kratos) is a microservice-oriented governance framework implemented by golang.
- [gin-gonic/gin](https://github.com/gin-gonic/gin) is a web framework written in Go.
